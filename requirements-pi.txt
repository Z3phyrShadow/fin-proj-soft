# ──────────────────────────────────────────────────────────────────────────────
# Raspberry Pi — install AFTER the base requirements.txt
#
#   pip install -r requirements.txt
#   pip install -r requirements-pi.txt
# ──────────────────────────────────────────────────────────────────────────────

# Official Pi Camera module support (replaces legacy picamera)
# Install via apt if pip fails:  sudo apt install python3-picamera2
picamera2>=0.3.12
